// Configure passport strategies here.

// Each strategy should be stored under a key matching the passport
// strategy package name without the 'passport-' prefix.
// I.e. package 'passport-google-oauth20' -> 'google-oauth20'

// See ./passport-strategies.example.js

const config = require('./index')
module.exports = {}
